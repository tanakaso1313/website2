{
  "name": "sotanaka-website",
  "version": "1.0.0",
  "description": "SOTANAKA portfolio and shop website",
  "scripts": {
    "dev": "vercel dev",
    "build": "npm run minify",
    "minify": "terser script.js -o script.min.js --compress --mangle --comments false"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "vercel": "^33.0.0"
  }
}
